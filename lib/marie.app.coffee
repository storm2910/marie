class App
	@name
	@path
	@cssProcessor
	@frontEndFramework
	@storage
	@templateEnegine
	@status
	@created
	@lastActive
	@PID


	constructor: (props) ->
		{@name, @path, @cssProcessor, @frontEndFramework, @storage, @templateEnegine, @status, @created, @lastActive} = props


	find: ->


	save: ->


	remove: ->


module.exports = App